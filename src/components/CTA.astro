<section class="cta-banner">
	<div class="cta-banner__inner">
		<div>
			<h2 class="cta-banner__title">Ready to ship faster?</h2>
			<p class="cta-banner__subtitle">
				Book a free 20-min discovery call.
			</p>
		</div>
		<a
			class="cta-banner__button"
			href="https://calendly.com/jalvarezp-codariolabs/30min"
			target="_blank">Book a Call</a
		>
	</div>
</section>

<style>
	.cta-banner {
		position: relative;
		padding-block: 4rem;
	}

	.cta-banner::before {
		content: "";
		position: absolute;
		inset: 0;
		background: linear-gradient(135deg, #3bafda 0%, #9be564 100%);
		overflow: hidden;
	}

	.cta-banner__inner {
		position: relative;
		z-index: 1;
		margin-inline: auto;
		max-width: 72rem;
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
		align-items: flex-start;
		padding-inline: clamp(1.5rem, 5vw, 3.5rem);
	}

	.cta-banner__title {
		font-family: var(
			--font-display,
			"Poppins",
			system-ui,
			-apple-system,
			sans-serif
		);
		font-size: clamp(2rem, 5vw, 2.75rem);
		font-weight: 600;
		color: #ffffff;
		margin: 0;
	}

	.cta-banner__subtitle {
		font-size: 1rem;
		color: rgba(255, 255, 255, 0.88);
		margin: 0;
	}

	.cta-banner__button {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		gap: 0.5rem;
		padding-inline: 2rem;
		padding-block: 0.9rem;
		border-radius: 999px;
		background-color: #ffffff;
		color: #0a2540;
		font-weight: 600;
		transition:
			transform 200ms cubic-bezier(0.16, 1, 0.3, 1),
			box-shadow 200ms cubic-bezier(0.16, 1, 0.3, 1);
		box-shadow: 0 18px 38px rgba(10, 37, 64, 0.18);
	}

	.cta-banner__button:hover,
	.cta-banner__button:focus-visible {
		transform: translateY(-2px);
		box-shadow: 0 22px 48px rgba(10, 37, 64, 0.25);
		outline: none;
	}

	@media (min-width: 768px) {
		.cta-banner__inner {
			flex-direction: row;
			justify-content: space-between;
			align-items: center;
		}
	}

	@media (prefers-reduced-motion: reduce) {
		.cta-banner__button {
			transition: none;
		}
	}
</style>
