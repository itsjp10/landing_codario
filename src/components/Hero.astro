---
import ParticlesBG from './ParticlesBG.jsx';
---

<section
	id="top"
	class="relative flex min-h-[90vh] items-center overflow-hidden bg-hero-gradient pb-24 pt-32 sm:pt-40 lg:pt-48"
>
	<ParticlesBG client:load />
	<div data-hero-blob aria-hidden="true" class="pointer-events-none absolute -top-24 left-1/2 h-[600px] w-[600px] -translate-x-1/2 rounded-full bg-brand-cyan/20 blur-3xl"></div>
	<div data-hero-blob aria-hidden="true" class="pointer-events-none absolute bottom-[-200px] left-[-200px] h-[520px] w-[520px] rounded-full bg-brand-lime/20 blur-3xl"></div>
	<div class="container relative z-10 grid gap-10 lg:grid-cols-[minmax(0,0.8fr)_minmax(0,0.6fr)] lg:items-center">
		<div class="space-y-8">
			<span class="inline-flex items-center gap-2 rounded-full border border-brand-dark/10 bg-white/70 px-4 py-2 text-xs font-semibold uppercase tracking-[0.32em] text-brand-dark/70 backdrop-blur-md">
				<span class="h-2 w-2 rounded-full bg-brand-lime"></span>
				Launch and scale faster
			</span>
			<h1 class="max-w-2xl text-4xl font-semibold leading-tight sm:text-5xl lg:text-6xl" data-hero-stagger>
				Build web experiences that launch and scale.
			</h1>
			<p class="max-w-xl text-lg text-brand-dark/70 sm:text-xl" data-hero-stagger>
				Codario Labs crafts modern web apps, management systems, and high-impact landing pages for startups and small businesses. We partner on strategy, build with velocity, and iterate with data.
			</p>
			<div class="flex flex-wrap items-center gap-4" data-hero-cta>
				<a class="btn btn-primary bg-brand-dark text-white hover:bg-brand-dark/90" href="#contact">
					Get a Free Audit
				</a>
				<a class="btn btn-secondary" href="#work">
					See Our Work
					<svg class="h-4 w-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6">
						<path d="M5 12h14" stroke-linecap="round" />
						<path d="m12 5 7 7-7 7" stroke-linecap="round" stroke-linejoin="round" />
					</svg>
				</a>
			</div>
			<div class="grid gap-6 pt-8 sm:grid-cols-3 sm:divide-x sm:divide-brand-dark/10">
				<div class="space-y-1">
					<p class="text-3xl font-semibold text-brand-dark">↑ 38%</p>
					<p class="text-xs font-semibold uppercase tracking-widest text-brand-dark/60">Avg. conversion lift</p>
				</div>
				<div class="space-y-1 sm:pl-6">
					<p class="text-3xl font-semibold text-brand-dark">Ship in days</p>
					<p class="text-xs font-semibold uppercase tracking-widest text-brand-dark/60">Not months</p>
				</div>
				<div class="space-y-1 sm:pl-6">
					<p class="text-3xl font-semibold text-brand-dark">99.9%</p>
					<p class="text-xs font-semibold uppercase tracking-widest text-brand-dark/60">Uptime on managed ops</p>
				</div>
			</div>
		</div>
		<div class="relative hidden min-h-[360px] rounded-3xl bg-white/70 p-8 shadow-soft backdrop-blur lg:flex lg:flex-col lg:justify-between">
			<div>
				<h2 class="text-2xl font-semibold text-brand-dark">What we ship</h2>
				<ul class="mt-6 space-y-4 text-sm text-brand-dark/70">
					<li class="flex items-start gap-3">
						<span class="mt-1 h-2.5 w-2.5 rounded-full bg-brand-lime"></span>
						<span>Conversion-first landing pages delivered in 10–14 days.</span>
					</li>
					<li class="flex items-start gap-3">
						<span class="mt-1 h-2.5 w-2.5 rounded-full bg-brand-cyan"></span>
						<span>Operational dashboards, portals, and management systems.</span>
					</li>
					<li class="flex items-start gap-3">
						<span class="mt-1 h-2.5 w-2.5 rounded-full bg-brand-dark"></span>
						<span>Growth retainers covering CRO, analytics, and iterative releases.</span>
					</li>
				</ul>
			</div>
			<div class="rounded-2xl border border-brand-dark/10 bg-brand-gray p-5">
				<p class="text-xs font-semibold uppercase tracking-[0.3em] text-brand-dark/60">Trusted by lean teams</p>
				<p class="mt-2 text-sm text-brand-dark/70">"Codario replatformed our product in six weeks with zero downtime and a measurable conversion bump."</p>
				<p class="mt-4 text-xs font-medium text-brand-dark/50">— Maya Chen, COO · OrbitOps</p>
			</div>
		</div>
	</div>

	<div aria-hidden="true" class="pointer-events-none absolute inset-x-0 bottom-0 h-32 bg-gradient-to-t from-brand-gray"></div>
</section>

<script type="module" is:inline>
	const reduced = document.documentElement.dataset.reducedMotion === 'true';
	if (!reduced) {
		import('gsap').then(({ gsap }) => {
			gsap.set('[data-hero-stagger]', { opacity: 0, y: 40 });
			gsap.set('[data-hero-cta]', { opacity: 0, y: 30 });

			gsap.to('[data-hero-stagger]', {
				opacity: 1,
				y: 0,
				stagger: 0.15,
				duration: 0.85,
				ease: 'power3.out',
			});

			gsap.to('[data-hero-cta]', {
				opacity: 1,
				y: 0,
				delay: 0.35,
				duration: 0.9,
				ease: 'power3.out',
			});
		});

		const blobElements = document.querySelectorAll('[data-hero-blob]');
		if (blobElements.length) {
			let frame = 0;
			const animateBlobs = () => {
				frame += 0.005;
				blobElements.forEach((blob, index) => {
					const x = Math.sin(frame + index) * 20;
					const y = Math.cos(frame + index) * 12;
					blob.style.transform = `translate(${x}px, ${y}px)`;
				});
				requestAnimationFrame(animateBlobs);
			};
			requestAnimationFrame(animateBlobs);
		}
	}
</script>
